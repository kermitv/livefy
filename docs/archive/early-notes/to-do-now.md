## Do Now

- Confirm Vite migration as the next build task
- Decide initial Vite target:
  - keep vanilla JS, or
  - move directly to React
- Choose first public deploy target for `dist/`
- Keep current Docker live-reload flow as fallback until Vite dev flow is verified
